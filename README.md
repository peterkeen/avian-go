# Static Site Template

This is a basic static site template for homelab-deployed static sites.

In this most basic form all it does is pack `public/` into a tarball and stuff it into forgejo's generic package registry.
